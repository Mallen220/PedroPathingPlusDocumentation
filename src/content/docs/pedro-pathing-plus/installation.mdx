---
title: Installation
description: Comprehensive guide on how to install and setup PedroPathingPlus for your FIRST Tech Challenge (FTC) project using Android Studio and Gradle.
head:
  - tag: meta
    attrs:
      name: keywords
      content: PedroPathingPlus installation, FTC Android Studio setup, Gradle dependencies, FTC robotics library, how to install PedroPathingPlus, FIRST Tech Challenge pathing
---

Welcome to the installation guide for PedroPathingPlus. This guide will walk you through the necessary steps to integrate our advanced autonomous pathing library into your **FIRST Tech Challenge (FTC)** robot controller project. By adding the correct Maven repositories and configuring your Gradle dependencies within Android Studio, you will unlock powerful trajectory generation and path following capabilities for your robot.

To use PedroPathingPlus in your FTC project, follow these steps:

## 1. Add Repositories

Add the following repositories to your `build.gradle` (Module: app) or `settings.gradle` file:

```groovy
maven { url "https://repo.dairy.foundation/releases" }

maven { url = 'https://mymaven.bylazar.com/releases' }

maven { url 'https://jitpack.io' }
```

## 2. Add Dependencies

Add the dependencies to your `build.gradle` (Module: app) dependencies block:

```groovy
dependencies {
    // PedroPathingPlus
    implementation 'com.github.Mallen220:PedroPathingPlus:master-SNAPSHOT' // or use a specific tag

    // Core Dependencies
    implementation 'com.pedropathing:ftc:2.0.0'
    implementation 'org.solverslib:core:0.3.3' // Will be replaced with PedproPathingPlus-specific version in future
    implementation 'org.solverslib:pedroPathing:0.3.3'
}
```

After adding these, sync your Gradle project to download the dependencies.
