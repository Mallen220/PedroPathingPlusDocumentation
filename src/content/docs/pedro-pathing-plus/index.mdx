---
title: Pedro Pathing Plus
description: Overview of Pedro Pathing Plus library
---

import { LinkCard, CardGrid } from '@astrojs/starlight/components';
import { getCollection } from 'astro:content';

export const docs = await getCollection('docs', ({ id }) => {
  return id.startsWith('pedro-pathing-plus/') && id !== 'pedro-pathing-plus/index.mdx';
});

Welcome to the documentation for the Pedro Pathing Plus library. Choose a topic below to get started:

<CardGrid>
  {docs.map(doc => (
    <LinkCard title={doc.data.title} href={`/${doc.slug}/`} />
  ))}
</CardGrid>
